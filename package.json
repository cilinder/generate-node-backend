{
  "name": "@cilinder/generate-node-backend",
  "version": "1.3.11",
  "description": "Project to generate a backend nodejs project template",
  "main": "src/index.ts",
  "bin": {
    "gnb": "./bin/generate-node-backend"
  },
  "scripts": {
    "test": "test",
    "build": "npx tsc -b && mkdir -p bin && cp templates/bin.template bin/generate-node-backend",
    "clean": "rm -rf build"
  },
  "keywords": [
    "generate",
    "nodejs",
    "template"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/cilinder/generate-node-backend"
  },
  "homepage": "https://github.com/cilinder/generate-node-backend#readme",
  "author": "cilinder",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^22.7.4",
    "@types/yargs": "^17.0.33",
    "typescript": "^5.6.2"
  },
  "dependencies": {
    "simple-git": "^3.27.0",
    "yargs": "^17.7.2"
  }
}
